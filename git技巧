 ***清除本地缓存
   git rm --cached -r . 
 
 ******git 一个分支完全覆盖另一个分支
   １，git push origin develop:master -f 
      就可以把本地的develop分支强制(-f)推送到远程master
  ２， 
   git checkout master // 切换到旧的分支 
   git reset –hard develop // 将本地的旧分支 master 重置成 develop 
  git push origin master –force // 再推送到远程仓库

***** git push
 git push的一般形式为 git push <远程主机名> <本地分支名>  <远程分支名>
 git push pandora_2.0_cmcc_voip pandora_2.0_cmcc_voip
 
 
 ****git修改本地的提交注释
git commit --amend

****回退commit  
  git reset --hard commit_id     //退到/进到 指定commit的sha码
  
 ****合入 patch 补丁  https://juejin.im/post/5a7a5860f265da4e8409175c
   git apply 
   
****git rebase
   https://www.codercto.com/a/45325.html
  
